import React from 'react'
import PropTypes from 'prop-types'

const NavigationBar = ({ shouldShowItems }) => {
    <nav>



    </nav>
}

NavigationBar.propTypes = {
    shouldShowItems: PropTypes.bool.isRequired
}

export default NavigationBar